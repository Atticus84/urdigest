import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'urdigest - Turn Instagram Saves into Daily Email Digests',
  description: 'Transform your saved Instagram posts into AI-summarized email digests delivered every morning.',
  keywords: 'Instagram, email digest, AI summary, saved posts, productivity',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  )
}
